import React from "react";
// import { makeStyles } from "@material-ui/core/styles";

// const useStyles = makeStyles((theme) => ({}));

export const Header = () => {
  //   const classes = useStyles();
  return <h2 className="mb-5">SpaceX Launch Programmes</h2>;
};

export default Header;
